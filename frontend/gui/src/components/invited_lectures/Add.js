import React from 'react';
import Form from './Form'

class LecAdd extends React.Component{
    render(){
        return (
            <Form type = 'add' btnText = 'Add'/>
        )
    }
}
    
export default LecAdd;